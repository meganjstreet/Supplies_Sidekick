<div class="container mb-3">
  <h3>Make an offer!</h3>
  <%= simple_form_for [supply, offer] do |f| %>
    <%= f.input :amount %>
    <%= f.input :details %>
    <%= f.button :submit, "Send offer", class: "btn btn-warning text-white" %>
  <% end %>
</div>
